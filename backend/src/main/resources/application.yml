spring:
  application:
    name: catering-planner
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/catering}
  jackson:
    default-property-inclusion: non_null

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health
